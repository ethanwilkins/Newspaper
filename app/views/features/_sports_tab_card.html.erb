<% if has_feature? :sports %>
	<div id="card">
		<div align="center">
			<p><%= link_to translate("Create a match"), new_sports_match_path %></p>
			<%= link_to translate("Create a team"), new_sports_team_path %>
		</div>
	</div>
<% end %>
