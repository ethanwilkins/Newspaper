// tips

var kind = '<%= kind %>';
var tip = $("#" + kind);

tip.one('click', function() {
	var param = $.param({
		kind: kind
	});
	$.ajax({
		type: "POST",
		url: "/tips/create",
		data: param
	});
});