<!-- users -->

<% if master? %>
	<% @users.each do |user| %>
		<div id="card">
			<%= link_to user.created_at, user_path(user), id: :top_right_link %>
			<div align="center" id="user_listing_name">
				<%= link_to user.name, user_path(user) %>
			</div>
		</div>
	<% end %>
<% end %>