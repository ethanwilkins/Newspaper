// messages

<% if @instant_messages.present? %>
	$("#instant_messaging_anchor").after("<%=j render 'messages/instant_messages' %>")
	$("#instant_messaging_anchor").remove() // removes the first anchor so the latest will be targeted
	$('#instant_messaging_anchor').scrollView();
	<% @instant_messages.update_all seen: true %>
<% end %>