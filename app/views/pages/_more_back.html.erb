<!-- pages -->

<!-- shows more until the end of the list -->

<div id="more_back">
	<% if all_items.size > page_size %>
		<% if items.size >= page_size %>
			<%= render "pages/more" %>
		<% else %>
			<%= render "pages/back" %>
		<% end %>
	<% end %>
</div>