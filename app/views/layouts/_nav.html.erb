<div class="navbar navbar-default navbar-fixed-top" role="navigation">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<%= link_to image_tag("elheroe.png", size: "60x30"), root_path,
				class: "navbar-brand", id: "elheroe_logo", title: translate("News and Local Events") %>
		</div>
		<div class="collapse navbar-collapse">
			<ul class="nav navbar-nav">
				<% unless current_user %>
				<li>
					<%= link_to translate("Log in"), root_url, title: translate("Log in if you've already signed up") %>
				</li>
				<li>
					<%= link_to translate("Sign up"), new_user_path, title: translate("Sign up to get more features") %>
				</li>
				<% else %>
				<li>
					<%= link_to translate("Games"), user_game_boards_path(current_user), title: translate("Play and win prizes") %>
				</li>
				<li>
					<%= link_to translate("Social"), tabs_path,
						title: translate("Post and share content specific to your interests and locale") %>
				</li>
				<li>
					<%= link_to notes_path, title: translate("View your notifications") do %>
						<% if current_user.notes.unchecked.size > 0 %>
							<div><%= translate("Notes") %> <span class="badge">
								<%= current_user.notes.unchecked.size %>
							</span></div>
						<% else %>
							<div><%= translate("Notes") %></div>
						<% end %>
					<% end %>
				</li>
				<% end %>
			</ul>
			<% if current_user %>
			<ul class="nav navbar-nav navbar-right">
				<li>
					<div class="navbar-form navbar-left" id="search_bar">
						<%= render "search/search_bar" %>
					</div>
				</li>
				<li>
					<%= link_to current_user.name.capitalize, user_path(current_user), title: translate("View your profile") %>
				</li>
				<% if privileged? %>
				<li>
					<%= link_to translate("Admin"), admin_path %>
				</li>
				<% end %>
				<li>
					<%= link_to translate("Log out"), sessions_destroy_path,
					data: { confirm: 'Are you sure you want to log out?' } %>
				</li>
			</ul>
			<% end %>
		</div><!--/.nav-collapse -->
	</div>
</div>