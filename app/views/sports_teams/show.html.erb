<!-- sports teams -->

<div id="card">
	<div align="center">
		<p><%= @team.name %></p>
		<p><%= translate("Wins") + ": #{@team.wins} · " + \
			translate("Losses") + ": #{@team.losses}" %></p>
		<div><%= translate("Total points") + ": #{@team.points}" %></div>
	</div>
</div>
