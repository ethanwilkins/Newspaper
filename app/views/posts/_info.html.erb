<% unless post.comments.empty? %>
	<%= link_to pluralize(post.comments.size, "comment"),
		post_path(post), id: :comment_link %>
<% else %>
	<%= link_to "Comment", post_path(post), id: :comment_link %>
<% end %>