post_id = <%= @post.id %>
link = $("#post_item_" + post_id)
link.attr("href", "/posts/up_vote/" + post_id)
link.text("<%= translate('Like') + (@post.up_votes.present? ? ' ' + @post.up_votes.size.to_s : '') %>")