<% if post.image.url %>
	</br><div align="center">
		<%= link_to image_tag(post.image, id: :post_image), post_path(post) %>
	</div>
<% end %>

<p align="center">
	<%= post.body %>
	<% post.hashtags.each do |tag| %>
		<%= link_to tag.tag, tagged_path(tag.tag) %>
		<%= " " %>
	<% end %>
</p>