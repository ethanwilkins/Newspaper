<% unless post.comments.empty? %>
	<%= link_to "Answer #{post.comments.size}", post_path(post), id: :bottom_left_link %>
<% else %>
	<%= link_to "Answer", post_path(post), id: :bottom_left_link %>
<% end %>