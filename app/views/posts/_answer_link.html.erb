<% unless post.comments.empty? %>
	<%= link_to pluralize(post.comments.size, "answer"),
		post_path(post), id: :comment_link %>
<% else %>
	<%= link_to "Answer", post_path(post), id: :comment_link %>
<% end %>