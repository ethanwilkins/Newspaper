<% unless post.comments.empty? %>
	<%= link_to translate("Answer") + " #{post.comments.size}", post_path(post), id: :bottom_left_link %>
<% else %>
	<%= link_to translate("Answer"), post_path(post), id: :bottom_left_link %>
<% end %>