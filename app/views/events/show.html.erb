<!-- events -->

<%= render "events/card", event: @event %>

<%= render "events/invitation", event: @event %>

<% @comments.each do |comment| %>
	<%= render "comments/card", comment: comment %>
<% end %>

<%= render "comments/new" if current_user %>