<div align="center">
	<%= button_to "Plan an event", new_event_path,
		method: :get, class: "btn btn-default btn-lg" %>
</div></br>

<% for event in @events %>
	<div id="card">
		<%= render "events/content", event: event %>
		<%= render "events/approve_deny", event: event %>
	</div></br>
<% end %>