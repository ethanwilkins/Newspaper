<% unless article.comments.empty? %>
	<%= link_to pluralize(article.comments.size, "comment"),
		article_path(article), id: :comment_link %>
<% else %>
	<%= link_to "Comment", article_path(article), id: :comment_link %>
<% end %>