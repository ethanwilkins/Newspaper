<!-- articles -->

<% if current_user %>
	<div align="center">
		<%= link_to events_path, class: "btn btn-default btn-lg",
			 title: translate("Find upcoming events in your area") do %>
			<span class="glyphicon glyphicon-calendar"></span> <%= translate("Local Events") %>
		<% end %>
	</div><br>
<% end %>

<%= render "articles/feed" %>

<%= render "pages/more_back", all_items: Article.articles, items: @articles %>

</br>
